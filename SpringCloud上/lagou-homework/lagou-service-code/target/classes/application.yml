server:
  port: 8081

spring:
  application:
    name: lagou-service-code

  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://47.92.156.231:3306/zdy_mybatis?useUnicode=true&characterEncoding=utf8
    username: root
    password: thor
  jpa:
    database: MySQL
    show-sql: true
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl #避免将驼峰命名转换为下划线命名

  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest

eureka:
  client:
    service-url:
      defalutZone: http://mytesta:8761/eureka/,http://mytestb:8762/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}:1.0-SNAPSHOT


management:
  endpoints:
    web:
      exposure:
        include: "*"